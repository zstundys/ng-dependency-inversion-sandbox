import { FormatDistanceToNowPipe } from './format-distance-to-now.pipe';

describe('FormatDistanceToNowPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDistanceToNowPipe();
    expect(pipe).toBeTruthy();
  });
});
